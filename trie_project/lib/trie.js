class Node {
    constructor() {
        this.children = {};
        this.isTerminal = false;
    }
}

class Trie {
   constructor() {
       this.root = new Node();
   }

   insertRecur(word) {

   }

   insertIter(word) {
       
   }

   searchRecur(word) {

   }

   searchIter(word) {

   }

   wordsWithPrefix(prefix) {

   }
}

module.exports = {
    Node,
    Trie
};